
Orchestration: Compose declares the full stack; health-condition depends_on guarantees readiness ordering; init-data fixes volume ownership for the non-root runtime user; internal DNS replaces host wiring.

Orchestration: Compose declares the full stack; health-condition depends_on guarantees readiness ordering; init-data fixes volume ownership for the non-root runtime user; internal DNS replaces host wiring.
